{
  "$comment": "Machine-readable Syrmos iOS/Android/Web parity ledger. Derived from docs/plans/2026-09-01-android-ios-parity-audit.md. status: matched | fixed | partial | backlog | intentional | blocked-external. Update when a row's parity changes.",
  "generatedFor": "2.0.0",
  "reference": "iOS is the product reference; Android/Web are brought to a deliberate-difference 1:1.",
  "deepQaSummary": {
    "asOf": "2026-09-02",
    "tally": {
      "fixed": 23,
      "userVisibleIntentionalDifferences": 0,
      "implementationOrCosmeticOnly_parityVerified": 3,
      "externalBlocked": 0,
      "total": 26
    },
    "tallyLegend": "fixed = user-visible parity achieved (some deploy-gated on the Pi). implementationOrCosmeticOnly = #10 (Android live-arrivals router built but intentionally unwired; both platforms show the same Airport strip), #11 (confidence chip wording; identical times/destinations) and #21 (map-motion engine); same rider-facing behaviour, so NOT counted as intentional user-visible differences. externalBlocked = 0: #10's OASA endpoint was re-verified LIVE on 2026-09-02 (serves airportArrivals minutesAway), so the only remaining part of #10 is a client-side wiring decision, not an external dependency. No user-visible intentional differences remain.",
    "resolvedInDeepQa": [
      7,
      9,
      12,
      13,
      14
    ],
    "remaining": {
      "externalBlocked": {},
      "implementationOrCosmeticOnly": {
        "10": "OASA endpoint re-verified LIVE 2026-09-02 (serves airportArrivals minutesAway for X93/95/96/97) and the client fetch layer exists; LiveArrivalsRouter is built + Koin-registered but intentionally unconsumed for GA, because iOS has no such router and wiring Android would create a divergence. Both platforms show the same Airport strip today - implementation-only dormant plumbing, not external-blocked.",
        "11": "confidence chip wording differs offline only; times/destinations identical - cosmetic",
        "21": "map-motion engine (CADisplayLink/coroutine/rAF); same train same place - implementation-only"
      }
    },
    "adversarialPasses": "Two adversarial passes. The first caught a HIGH silent no-op in the #12 server override (wrong column s.name vs the real name_en, masked by a test fixture; fixed #83) and a MEDIUM latent /api/lines position collision (fixed #84). The second reviewed the fixes clean. A deep 7-state user-visible #9/#11 analysis then caught a HARD phantom-rows defect (server airport-only guard used non-existent ids) and a soft rounding gap, both fixed #89.",
    "serverFixesDeployGated": "#12 short-turn destinations, #9 phantom-rows + rounding are correct + test-verified but take effect only on the next Pi deploy (deploy.sh, SSH to the LAN Pi, which the sandbox safety layer blocks). Production endpoint verified: currently serves the pre-fix behaviour (M1 last train -> Kifissia, 0 lastTrains).",
    "releaseEngineering": {
      "iosPrivacyManifest": "RESOLVED. PrivacyInfo.xcprivacy added to all 4 bundles (app/widget/watch/complications) with reason codes verified against Apple's docs JSON (UserDefaults CA92.1+1C8F.1, DiskSpace E174.1), wired via the xcodeproj tool. Proven embedded in the archive; the signed IPA passes altool --validate-app (no ITMS-91053).",
      "iosSignedIpa": "SIGNED IPA VERIFIED + APP STORE VALIDATION VERIFIED via the CI dry-run (real Apple Distribution cert + App Store profiles; 'VERIFY SUCCEEDED with no errors'). TestFlight upload skipped (human-gated).",
      "androidAab": "PRODUCTION-SIGNED AAB VERIFIED via the CI dry-run. Production signing certificate matched the expected CI configuration (VERIFIED); fingerprint intentionally not recorded here. Play upload skipped (human-gated). Local throwaway-signed build also validated bundle contents.",
      "security": "CLEAN. No committed secrets (all env), ATS enforced, export compliance declared, no debuggable/debug-entitlement leaks, no TLS bypass, no dev flags.",
      "storeMetadata": "App icons + release notes + usage descriptions + export compliance present. Privacy-policy URL RESOLVED: https://syrmos.peterdsp.dev/privacy is LIVE (HTTP 200, standalone page, renders without JS, desktop + mobile verified) and linked in-app from Settings/About on iOS + Android and from the web map info panel. Store declaration matrix prepared in docs/ops/STORE-PRIVACY-DECLARATIONS-2.0.0.md; entering it in App Store Connect + Play Console Data Safety remains human/account-gated.",
      "humanGated": [
        "push v2.0.0 tag (starts signed store uploads)",
        "TestFlight upload",
        "Google Play upload",
        "enter the (already-live) privacy-policy URL https://syrmos.peterdsp.dev/privacy + the Data Safety / App Privacy labels in the store consoles",
        "deploy the server fixes to the Pi (migration 0017 + scraper + projector for #12/#9)"
      ]
    }
  },
  "features": [
    {"id": 1, "area": "schedule-sync", "expected": "Per-line etag diff pulls only changed bundles so timetables update without an app release", "platforms": ["ios", "android", "web"], "status": "fixed", "pr": 52, "tests": "ScheduleSyncRepositoryTest"},
    {"id": 2, "area": "interchange", "expected": "Proximity (150m) interchange with actionable per-line transfer targets", "platforms": ["ios", "android"], "status": "fixed", "pr": 58, "tests": "InterchangeResolver tests"},
    {"id": 3, "area": "timetable-holiday", "expected": "Greek holiday calendar applied on the offline seed day-type path", "platforms": ["ios", "android"], "status": "fixed", "pr": 54, "tests": "ServiceDayResolverTest"},
    {"id": 4, "area": "search-fields", "expected": "Search matches name/nameEl/nameSq/id", "platforms": ["ios", "android"], "status": "fixed", "pr": 55, "tests": "StationSearchMatchTest"},
    {"id": 5, "area": "data-validDates", "expected": "Date-scoped trips run only on their valid ISO dates", "platforms": ["ios", "android"], "status": "fixed", "pr": 63, "tests": "TrainSimulatorTest validDates cases"},
    {"id": 6, "area": "home-location-cta", "expected": "Denied/empty location shows an enable-location CTA", "platforms": ["ios", "android"], "status": "fixed", "pr": 77, "note": "NearbyLocationCta in the empty branch; verified on emulator"},
    {"id": 7, "area": "home-refresh-connectivity", "expected": "Pull-to-refresh + event-driven reconnect retry", "platforms": ["ios", "android"], "status": "fixed", "pr": 82, "note": "pull-to-refresh + retry signal (#60); ConnectivityObserver now calls LiveDataFreshness.requestRetry() on network onAvailable, mirroring the iOS NWPathMonitor. Verified on emulator: the callback fires on an airplane-off reconnect and drives the home probe, no crash."},
    {"id": 8, "area": "line-detail", "expected": "Metro/tram line detail shows live trains + upcoming departures, localized", "platforms": ["ios", "android"], "status": "fixed", "pr": 74, "note": "title/count localized (#59); upcoming-departures fallback for every line (#74), verified on emulator"},
    {"id": 9, "area": "departures-source", "expected": "One source-selection policy (local-first for Athens)", "platforms": ["ios", "android", "web"], "status": "fixed", "pr": 89, "note": "A deep 7-state user-visible analysis (fresh/stale/down server x cache present/missing x remote/seed newer) found the architectural local-first (iOS) vs server-first (Android/Web) split produces at most a data-version-skew difference behind an identical chip in rare states - iOS deliberately stays local-first for Athens to avoid a drifted/spoofed server clock overwriting a correct offline answer, so this is a defensible design difference, not a bug. It DID surface two concrete rider-facing defects, both fixed in #89: (1) HARD - the server airport-only guard used non-existent ids M3_PEK/M3_KRP instead of M3_PEA/M3_KO2, so Android/Web showed phantom 'towards Doukissis Plakentias' rows at Peania-Kantza/Koropi that iOS never showed; (2) SOFT - the server rounded banker's while all three clients round half-up, a <=1 min disagreement on half-integer-headway slots. Remaining known limitation (post-2.0 fast-follow, edge case): web shows no departures on a true offline cold-start (no bundled per-line band seed, unlike iOS/Android). Server fixes are deploy-gated on the Pi (same path as #12)."},
    {"id": 10, "area": "live-arrivals-router", "expected": "LIVE->SCHEDULED fallback wired as first tier (X93-97 live ETAs)", "platforms": ["android"], "status": "intentional", "note": "NOT external-blocked (re-verified 2026-09-02). The Pi endpoint api-syrmos.peterdsp.dev/api/oasa-airport-buses is LIVE and serves the exact live ETAs the router needs: airportArrivals[] with minutesAway per line (verified: 7 arrivals, X93/X95/X96/X97, updatedAt seconds old), plus vehicles[] positions. The client fetch layer exists and is Koin-registered (OasaAirportBusService.fetchAirportBuses(), NetworkModule). LiveArrivalsRouter + providers are also built + Koin-registered, but no consumer resolves the router and GetNextDeparturesUseCase does not take it, so the live ETAs are not surfaced in the Airport departures list. This is DELIBERATE for GA: iOS has no equivalent router, so wiring Android to show live airport-bus ETAs would CREATE an iOS/Android divergence rather than remove one. Rider-facing parity therefore holds today (both platforms show the scheduled/check-OASA Airport strip). Implementation-only dormant plumbing, tied to the #9 source-policy decision; a post-GA follow-up, not a blocker."},
    {"id": 11, "area": "source-confidence", "expected": "One confidence rule shared by all clients", "platforms": ["ios", "android"], "status": "intentional", "note": "COSMETIC, not a rider-facing defect (confirmed by the deep analysis): for the SAME train at the SAME time the displayed time and destination are identical across platforms; only the confidence CHIP wording differs offline (iOS 'Scheduled', Android 'Estimated', Web 'Offline snapshot'). Online all three agree ('Scheduled'). No rider is directed to a wrong train by the chip. iOS's blanket .scheduled is the outlier (arguably should say estimated offline) but is not misdirecting. Left as a deliberate soft/labeling difference; unifying it is optional polish, not a correctness fix."},
    {"id": 12, "area": "projector-lastTrains", "expected": "Short-turn last-train destination override in the shared projector", "platforms": ["ios", "android", "web"], "status": "fixed", "pr": "80+83", "note": "Fixed server-side. project_next_departures now applies the last_train_endpoints short-turn override (M1 00:30 -> Omonia, not Kifissia). Android and Web are server-first so they inherit it with no client change; iOS was already correct via its synced bundle. NOTE: the adversarial pass caught that the initial fix (#80) queried a nonexistent stations.name column (production has name_en/name_el), so the OperationalError guard swallowed it and the override was a silent no-op in production; #83 corrects it to name_en and rebuilds the test fixture to mirror the production schema so a wrong column now fails the tests. Offline (bundled seed ships zero short-turn rows) every platform shows the line terminal, consistent. Deploy note: takes effect once the Pi applies migration 0017 and the STASY scraper populates the table; a verified no-op until then. The KMP/Web client fallback band-projectors do NOT mirror the override yet (a documented fast-follow, no-op offline since the seed is empty)."},
    {"id": 13, "area": "strings", "expected": "Canonical copy per key across platforms", "platforms": ["ios", "android"], "status": "fixed", "pr": "56+86", "note": "subtitle + news header aligned (#56). #86 localizes the Airport route-strip station names (M3/A1/X95/X97), which were hardcoded English. NOTE: an earlier deep-QA note called this a seed-DATA gap (nameEl=null for M3_DIM/M3_DOY); that was a false alarm from checking the camelCase field - the seed uses name_el and every station has complete name_el/name_sq, so #86 pulls the exact seed values through airportText(). Verified on the emulator in Greek. Other Airport-tab hardcodes remain SHARED with iOS (not a divergence)."},
    {"id": 14, "area": "settings", "expected": "Recents store, refresh feedback, digest toggle wired or hidden", "platforms": ["ios", "android"], "status": "fixed", "pr": 81, "note": "The Android morning-digest toggle only persisted notif_morning_digest with no consumer (dead UI); hidden for GA (satisfies the 'wired or hidden' criterion), verified on emulator. Refresh feedback already present on Android (Last updated + Check now). Recents store is an iOS-only feature (absent on Android/Web) tracked as a separate post-2.0 feature, not dead UI."},
    {"id": 15, "area": "networking-timeouts", "expected": "Fast per-call timeouts on live/departures", "platforms": ["ios", "android"], "status": "fixed", "pr": 70, "tests": "NetworkTimeoutsTest"},
    {"id": 16, "area": "api-lines-semantics", "expected": "Canonical /api/lines behaviour (overlay vs upsert)", "platforms": ["ios", "android", "web"], "status": "fixed", "pr": "76+84", "note": "LinesRefresher overlay-only (matches iOS); strictly write-reducing. #84 (adversarial-pass finding) fixes a latent persistence bug: a novel stop attaching to a seeded line used its payload index as position_on_line, which collided with a seeded stop on a mid-line insert and made ORDER BY nondeterministic; membershipPosition now parks overlay-added stops past every seeded position (collision-free, deterministic)."},
    {"id": 17, "area": "search-diacritics", "expected": "Accent/tonos-insensitive search", "platforms": ["ios", "android"], "status": "fixed", "pr": 65, "tests": "TextExtensionsTest, StationSearchMatchTest", "note": "shared normalizer; iOS can adopt it"},
    {"id": 18, "area": "a11y-departures", "expected": "Compound departure label + heading + alert live-region", "platforms": ["ios", "android"], "status": "fixed", "pr": 75, "note": "DepartureCard merged into one TalkBack node + section headings; verified via a11y node tree"},
    {"id": 19, "area": "news-offline", "expected": "News keeps last good payload offline", "platforms": ["ios", "android"], "status": "fixed", "pr": 67, "tests": "RailNewsRepositoryTest, RailNewsItemSerializationTest"},
    {"id": 20, "area": "athens-time", "expected": "Athens time via the IANA tz everywhere", "platforms": ["ios", "android"], "status": "fixed", "pr": 66, "tests": "DateTimeExtensionsTest"},
    {"id": 21, "area": "map-cosmetics", "expected": "Aligned map padding / vehicle motion", "platforms": ["ios", "android"], "status": "intentional", "classification": "PARITY VERIFIED - IMPLEMENTATION-ONLY DIFFERENCE (not a user-visible intentional difference)", "note": "The rider sees the same train in the same place at the same wall-clock. Vehicle motion is driven by CADisplayLink on iOS (native, display-synced), a per-tick coroutine on Compose/Android, and requestAnimationFrame on web; all three interpolate the SAME projected positions along the SAME shapes, so user-visible behaviour is equivalent. Matching engines is neither possible (no CADisplayLink on Android/web) nor better. Under the user-visible-behaviour rule this is NOT a parity difference and should not be counted as an intentional user-visible difference; it is an implementation detail. (A minor map-padding tweak remains cosmetic backlog.)"},
    {"id": 22, "area": "ios-live-map-null", "expected": "One GPS-less train does not blank the live map", "platforms": ["ios"], "status": "fixed", "pr": 61},
    {"id": 23, "area": "ios-loop-closure", "expected": "Circular bus routes draw their return leg", "platforms": ["ios"], "status": "fixed", "pr": 69, "tests": "MapRouteGeometryTests"},
    {"id": 24, "area": "ios-announcement-decode", "expected": "One malformed announcement does not void the feed", "platforms": ["ios"], "status": "fixed", "pr": 62},
    {"id": 25, "area": "live-decode-resilience", "expected": "One malformed live/offset row does not drop the whole layer", "platforms": ["android"], "status": "fixed", "pr": 64, "tests": "SyrmosLivePositionsDecodeTest"},
    {"id": "airport-interactive", "area": "airport-tab", "expected": "Airport service tiles/rows/legs open the stop's Station Detail", "platforms": ["ios", "android"], "status": "fixed", "pr": 68, "note": "buses inert pending their timetable data"}
  ]
}
