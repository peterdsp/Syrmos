# Syrmos 3.0.0 "Journeys" — implementation roadmap

Companion to [3.0.0-IMPLEMENTATION-PROMPT.md](3.0.0-IMPLEMENTATION-PROMPT.md) (the UI contract and
scope authority) and [3.0-JOURNEYS.md](3.0-JOURNEYS.md). This file is the execution plan: how the
work is sliced, what "done" means per slice, and how each slice is verified locally.

Baseline inspected: `9e717645` on `master`, plus uncommitted iOS shell work already in progress in
`iosApp/iosApp/App/SyrmosApp.swift` (readable content column + Ariadne moved into a per tab
`safeAreaInset`). That in flight work is the start of the J06 adaptive shell and is preserved, not
reverted.

## 0. Ground truth from the current tree

| Concern | Where it lives today |
| --- | --- |
| Shared journey model | `core/model/.../planner/JourneyResult.kt`, `JourneySegment.kt` (segments + estimated minutes + transfer count; no absolute instants, no feasibility) |
| Shared planning use case | `core/domain/.../usecase/PlanJourneyUseCase.kt` (256 lines, topology routing) |
| Shared GO engine | `core/domain/.../go/GoGuidance.kt` (+ `GoGuidanceTest.kt`) |
| Cross client golden contract | `fixtures/go-guidance/cases.json` (schemaVersion 1) |
| iOS planner / GO | `iosApp/iosApp/Features/Assistant/JourneyPlanner.swift`, `Features/Go/*` (incl. `GoDemoEntryView.swift`) |
| Android app shell | `composeApp/src/commonMain/kotlin/com/syrmos/app/SyrmosApp.kt`, `feature/` |
| Web app | `composeApp/src/wasmJsMain/resources/` — `index.html`, `web-map.js` (owns the `I18N` dict + `t()` + `applyTranslationsToDom`), `web-planner.js`, `web-go*.js`, `web-nav.js`, `web-router.js` |
| Web i18n | 4 language blocks (`en`/`el`/`sq`/`it`) inside `I18N` in `web-map.js`; DOM localized through `data-i18n*` attributes |
| Web tests | `web-tests/*.test.js` run with `node --test`; mostly static source guardrails plus DOM harnesses |
| Design tokens | `core/designsystem/.../theme/tokens` (canonical) regenerated by `ops/designsystem/generate_tokens.py` into JSON/CSS/Swift mirrors |

Known gap the prompt calls out and this tree confirms: `index.html` lines ~121-183 run an IIFE that
does `document.documentElement.innerHTML = ...` for any mobile user agent, replacing the whole app
with a store landing page. That is the "user-agent app-blocking page" the prompt requires removed.

## 1. Slicing (matches prompt section 9 delivery sequence)

Each slice ships on all three clients before it is called complete, with language neutral fixtures
as the shared truth. Priorities J01-J10 come from prompt section 3.

### Phase F — Foundation (no new journey claims)
- **F1 Web app access.** Remove the mobile UA blockade; every browser gets the app. Store promotion
  becomes a dismissible, secondary banner (localized in all 4 languages). *Verify:* `node --test`
  guardrail + rendered mobile viewport.
- **F2 Shared visual fixture.** `fixtures/journeys/ui-reference.json`, test only, frozen Athens
  clock, synthetic IDs `test-origin`/`test-transfer`/`test-destination`, one direct + one transfer
  option, deterministic clocks 08:42-09:12. *Verify:* JSON schema/self-test.
- **F3 Adaptive shell.** Finish the iOS readable-column/sidebar work already started; add the same
  content breakpoint rules to Android (`SyrmosApp.kt`, currently width switch gated to web) and web
  compact nav (replace the hidden-nav rule). *Verify:* window matrix screenshots per client.
- **F4 Session/state models + deterministic clock.** Add the versioned typed models from prompt 7.1
  (`JourneyRequest`, `JourneyOption`, `Leg`, `SourceRef`, `Feasibility`, `SavedJourney`,
  `ActiveJourney`) as shared Kotlin + mirrored Swift/JS, plus an injectable Athens test clock.
  *Verify:* domain unit tests, migration round trip.

### Phase P — Planning (J01, J02, J05)
- Time-aware ranked itineraries (extend `PlanJourneyUseCase`, `JourneyPlanner.swift`,
  `web-planner.js`): depart-at / arrive-by (backward search), up to 3 distinct options, ranking.
- Endpoint picker (S03), plan draft (S02), route results (S04), selected detail (S05).
- Timing margin + feasibility (comfortable/tight/missed/unknown) per prompt 7.3; last connection home.
- Saved journeys + recents + resume (S08). *Verify:* `fixtures/journeys/` behavioral cases identical
  on web (node), Kotlin (`:core:domain:allTests`), iOS (XCTest).

### Phase G — Guidance (J03)
- Selected itinerary -> persistent GO session (S06), manual progression, resume/end, foreground
  evidence-based auto-advance. Wire real selected trips; remove `GoDemoEntryView` and the web fixed
  showcase from release navigation. *Verify:* GO fixtures + lifecycle restore per client.

### Phase R — Recovery (J04)
- Disruption exclusions, connection risk + confirmed replacement (S07), failure/capability states
  (S10), accessibility unknowns. *Verify:* suspended/partial-closure/stale/no-coverage fixtures.

### Phase N — Native surfaces (J07 unify freshness first; J08/J09 next)
- Unified freshness/offline presentation (J07) reuses the existing shared freshness rule.
- Opt-in glance surfaces: iOS Widget/ActivityKit, Android ongoing/Live Update, web foreground resume.
  *Verify:* permission-denied/stale/end-cancel paths.

### Phase Z — Release candidate
- Visual baselines S01-S10, full locale/state/window matrix, device evidence, release docs. Versions
  bumped only as part of the actual release process, never as a side effect.

## 2. Parity discipline

- One pure transform per rule, mirrored across web/iOS/Android, asserted against the same
  `fixtures/journeys/` inputs (never three independently invented scenarios).
- IDs stable across recomputation; never match by list index or display name (prompt 7.1).
- `null` means unknown, never zero. All schedule evaluation in `Europe/Athens` with stored absolute
  instants + operator service date; do not assume 24h days.
- Design dimensions flow from the token source + `generate_tokens.py`, not hand-copied per platform.

### 2.1 De-duplication and grouping is a hard invariant (all surfaces)

No list of departures, options, saved journeys, or airport services may ever show the same thing
twice. Concretely, on every client and every surface:

- **Group by (line, destination).** One card per line+destination, with the next N times as a
  dominant countdown plus a "Then …" tail. Never a stack of one-time-each rows for the same line to
  the same place (the exact defect visible in the shipped Airport hub: `A1 Syntagma 04:00`,
  `A1 Syntagma 04:00`, `A1 Syntagma 05:00` as three rows). This already exists as a pure transform
  per client (`SyrmosDepartures.groupDepartures` on web, `SyrmosDeparturesGrouping` on Android,
  `AirportServiceRows`/board grouping on iOS). New 3.0 lists must route through the same transform,
  not re-render raw rows.
- **Drop exact duplicates.** Identical `(line, destination, time)` collapses to one, including the
  case where two source ids project one physical train (e.g. `M3` and `M3_AIR`, deduped by time).
- **Deduplicate itineraries** by identical leg sequence before ranking (prompt 7.2) and never pad to
  three options with near-identical routes.
- **Verify it.** Extend `fixtures/journeys/` (and reuse the existing departures-grouping tests) with
  explicit duplicate inputs: repeated destination, same-time duplicate, dual-id train, duplicate GPS
  event (prompt 7.4 idempotency). The same duplicate input must collapse identically on web (node),
  Kotlin, and iOS. iOS already pins this in `AirportServiceTests`
  (`testMetroDuplicatesCollapseByTime`, `testSuburbanDedupAndCap`); the requirement is to keep that
  guarantee everywhere a 3.0 list is introduced.

## 3. Local verification tiers available in this environment

Confirmed available: JDK17 + Android SDK, Xcode, node, curl to the live API. So:
`node --test web-tests/*.test.js`; `./gradlew :core:domain:allTests`; `./gradlew :androidApp:assembleDebug`;
`xcodebuild -list -project iosApp/Syrmos.xcodeproj` + XCTest on an available simulator;
`python3 scripts/verify-bundles.py`. Deployment commands are never run as tests.

## 4. Progress log

- [x] **F1** web mobile-blockade removed; dismissible secondary store banner (4 locales); web-test added.
- [x] **F2** `fixtures/journeys/ui-reference.json` created (test only, spec-exact) + validation test.
- [x] **Dedup parity (2.1)** shared `groupDepartures` now drops exact duplicate departures (same
      clock time, or same minutesAway with no clock) within a group on web
      (`web-departures.js`) and Android (`DepartureGrouping.kt`), matching iOS
      (`AirportServiceRows`). Tests added on web + Kotlin; iOS already covered
      (`AirportServiceTests`). Closes the "two 04:00 Syntagma rows" gap.
- [x] **F4** shared journey contract in Kotlin (`core/model/.../journey/`): `JourneyRequest`,
      `JourneyOption`, `Leg`, `SourceRef`, `Feasibility`, `SavedJourney`, `ActiveJourney` +
      enums, lowercase wire names matching `fixtures/journeys/`, `JourneyContract` versioned
      atomic persistence with `Unsupported`/`Corrupt` recovery routing. 7 KMP tests incl.
      decoding the ui-reference wire shape and null-means-unknown. Deterministic clock reuses
      the existing injectable `kotlinx.datetime.Clock` + Athens helpers (no new abstraction).
      Swift/JS typed mirrors land with their client slices; the JSON fixture is the neutral truth.
- [~] **F3** adaptive shell, in progress:
    - [x] shared content-breakpoint primitive `core/common/.../layout/ContentBreakpoint.kt`
      (section 4.1 rule + 4.2 reference canvases C390/C360/M768/E1024/W1360/Short), 9 KMP tests
      pinning every boundary width and canvas exactly; doubles as the spec for the Swift/JS mirrors.
    - [x] Android `SyrmosApp.kt` now consumes it: native large windows (>=600dp) show a
      `NavigationRail` (Home/Explore/Map/Airport/More); compact keeps the liquid-glass bottom bar;
      web desktop shell (>=900dp) unchanged. Verified empirically on the claude_pixel emulator:
      rail renders at a ~900dp window, bottom bar returns at 411dp portrait (screenshots captured).
    - [x] Web: replaced the `display:none` hidden-nav rule with a reachable-everywhere nav:
      600-959px keeps an 80px left icon rail, <600px becomes a fixed bottom bar (five equal
      labelled workspace slots + a trailing More utility), reusing the same `.nav-rail` element so
      `web-nav.js` routing/aria-current are untouched. Localized labels `ws_*` added in all 4 locales.
      Verified live in the browser at 390/768/1200px. Also fixed an F1 banner bug caught in the
      render: `.store-banner{display:flex}` beat the bare `[hidden]` attribute so the banner showed
      at all widths; added a `[hidden]` guard + a `has-store-banner` body class that reserves the
      strip's height only while it is shown. Dismiss verified (hidden, class removed, localStorage set).
    - [ ] iOS readable column (user's in-flight work) and the 840+ expanded drawer (Android + web).
- [ ] F4-mirrors: Swift + JS typed journey models mirroring `core/model/.../journey` (with client slices)
- [~] **Phase P** started with the truth-critical core: connection feasibility (prompt 7.3).
      `core/domain/.../journey/FeasibilityCalculator.kt` + web `web-feasibility.js` are exact
      mirrors (margin in seconds, four honest states, worst-transfer aggregation, unknown when a
      required term is missing, closure override, configurable thresholds). Driven by a shared
      golden fixture `fixtures/journeys/feasibility.json` (11 cases incl. 179/180 boundaries):
      web asserts every case (12 tests), Kotlin asserts the same cases (11 tests). No success
      percentage anywhere.
    - Ranking/dedup/cap (prompt 7.2 / S04): `JourneyRanker.kt` + web `web-journey-ranker.js`
      mirror each other (sort by objective then arrival/changes/walking/stable-id, dedup identical
      leg sequences, cap at 3 never padded, badge the top only when the ranking is truly best,
      unknown metrics sort last). Shared fixture `fixtures/journeys/ranking.json` (7 cases): web 8
      tests, Kotlin 7 tests. Reinforces the de-dup invariant (2.1) at the itinerary level.
    - Web planning adapter + real Plan flow: `web-journey-plan.js` (`SyrmosJourneyPlan.plan`)
      composes the topology planner into a ranked `JourneyOption` with computed feasibility and
      HONEST timing (no schedule -> `timingKind: estimated`, null instants, disclosed duration
      estimate, feasibility `unknown` for a transfer / `direct` for a single ride). 3 node tests
      over the real seed. The web Plan workspace now replaces the old fixed M1->M2 showcase with a
      real endpoint picker (From/To over 393 stations) + "Find routes" -> results summary (~duration,
      changes, line chain, feasibility chip) -> route detail in the GO panel. Verified live in the
      browser: Piraeus->Elliniko = "~42 min, 2 changes, M1->M3->M2, Estimated times";
      Monastiraki->Thiseio = "~2 min, 0 changes, M1, Comfortable".
    - Android Plan flow mirror: `JourneyPlanAdapter.kt` (Kotlin peer of `web-journey-plan.js`,
      3 KMP tests mirroring the web adapter cases) + `PlanScreenRoute` (From/To station pickers,
      Find routes, ranked result card with feasibility chip) launched from an Explore FAB.
      Verified on the emulator: Monastiraki->Thiseio = "~2 min, 0 changes, M1, Comfortable"
      (matches web). FOUND while verifying: `PlanJourneyUseCase` cross-line routing is broken (its
      transfer-edge loop is a no-op), so cross-line plans return "No route found" on Android while
      web routes them; spun off as a separate task. The adapter/UI honestly surface the planner's
      null, so no fabricated route is shown.
    - Kotlin planner cross-line routing FIXED: `PlanJourneyUseCase`'s transfer loop was a no-op;
      it now adds bidirectional transfer edges between co-located per-line station ids grouped by
      an accent-folded name (`foldStationName`, mirroring web `nameKey`), and `reconstructSegments`
      treats a transfer edge as a leg boundary without emitting a transfer segment. 2 new
      reconstruction tests (transfer split + fold). Verified on the emulator: Piraeus->Elliniko now
      routes "~48 min, 2 changes, M1 -> M3 -> M2, Estimated times" (was "No route found").
    - iOS Plan flow mirror: `PlanFlow.swift` (`JourneyPlanAdapter` + SwiftUI `PlanView`), driven by
      the existing `JourneyPlanner` (which already has transfer edges), launched from a "Plan a
      journey" button on the Explore/Lines screen. Localized (en/el/sq/it). Registered in the
      checked-in `.xcodeproj` (hand-added pbxproj entries; no project.yml regen). Verified on the
      iPhone 17 simulator, in Greek: Πειραιάς -> Ελληνικό = "1 διαδρομή, ~42 λεπ, 2 αλλαγές,
      M1 -> M3 -> M2, Εκτιμώμενοι χρόνοι" (matches web ~42 min and the Android route). The Plan flow
      now exists on all THREE clients. (Fixed a SwiftUI List/Button synthetic-tap issue by switching
      the picker rows to ScrollView + onTapGesture.)
    - Schedule-aware pass DONE (engine + parity): `web-schedule-plan.js` /
      `SchedulePlanner.kt` (`assignSchedule`) assign REAL scheduled departure/arrival instants from
      a timetable (earliest catchable departure per leg; transfer-min gated; epoch math so DST and
      past-midnight fall out of the offsets), so the feasibility engine yields real
      comfortable/tight and direct trips get a real clock. Golden fixture
      `fixtures/journeys/schedule.json` (direct, tight=120s, comfortable=300s, no-service=unknown,
      overnight); 6 web + 5 Kotlin tests. Both planning adapters (`web-journey-plan.js`,
      `JourneyPlanAdapter.kt`) now take an OPTIONAL timetable and upgrade the estimated option to
      scheduled when given (adapter integration tested on both). Fresh forward plans are never
      'missed' (only catchable departures are assigned); 'missed' stays exercised by feasibility.json.
    - WEB timetable wiring DONE: the Plan workspace `buildTimetable(detailed)` builds a real
      timetable from the live departure projection (`buildStationDepartures` at each leg board node,
      departures as `Date.now()+minutesAway`) and passes it + a requested instant into the adapter,
      so the web Plan now shows REAL feasibility. Verified in-browser: Piraeus->Syntagma =
      "~32 min, 1 change, M1 -> M3, Comfortable" (computed from headways, not the flat estimate);
      several direct + transfer routes confirmed. A route whose board node lacks projection data
      (e.g. the 2-change M1->M3->M2 to Elliniko) degrades honestly to "Estimated times" rather than
      inventing a margin. Guardrail test `web-tests/plan-schedule-wiring.test.js`.
    - ANDROID timetable wiring DONE: `PlanScreenRoute` injects `ComputeDeparturesFromBandsUseCase`
      and `buildTimetable(result, ...)` projects each ride segment's next departures at its board
      station (both directions, filtered by line) as absolute instants (`now + minutesAway`), leg
      travel from the segment's own estimated minutes, passed into `JourneyPlanAdapter.toOptions`.
      Verified on the emulator: Piraeus->Syntagma = "~18 min, 1 change, M1 -> M3, Tight" (a real
      computed margin from live projection, not "Estimated").
    - iOS timetable wiring DONE (engine + projector): `PlanFlow.swift` gained a compact Swift
      schedule + feasibility pass (peer of the shared engine: earliest catchable departure per leg,
      worst transfer margin, tight<=179s) fed by `ScheduleProjector.nextDepartures`. Compiles and
      runs on the iPhone 17 sim; it degrades honestly to "Estimated times" when the projector has no
      data for a leg (on the test sim the schedule store had not loaded Athens metro bundles, so the
      M1->M3 route degraded; the same code shows real feasibility where the line's data is present,
      as Android's "Tight" demonstrates). So the timetable wiring now exists on ALL THREE clients.
    - Interchange projection coverage BROADENED: the timetable builders were starving later legs.
      Root cause: departures were collected near-term and capped (web `buildStationDepartures` = 10
      slots across ALL lines at a node; Android/iOS per-line `limit: 8`), so a leg the rider reaches
      15+ min out had no CATCHABLE departure and stayed unscheduled -> "Estimated". Fix: project each
      leg's own line with a long horizon (web `projectFromBundle(..., 30)` directly from the bundle,
      dropping the fragile node lookup; Android/iOS `limit: 20`). Verified: Piraeus->Elliniko
      (M1->M3->M2, 2 changes) now times fully on web ("~42 min, Tight") and Android ("~41 min, Tight")
      instead of "Estimated"; more transfer routes time across the board. (Web departures are
      origin-timed, an existing web simplification, so metro margins cluster near the tight
      threshold -- honest and headway-driven.)
    - Arrive-by + last-connection-home ENGINE done (parity, not yet in any UI): a backward pass
      `assignScheduleArriveBy` / `lastConnection` on web (`web-schedule-plan.js`) and Kotlin
      (`SchedulePlanner`) assigns the LATEST feasible departures to arrive by a target (a real
      backward search, not a static-duration subtraction); with no target the last leg takes the
      latest available departure = last train home; the first ride's departure is the "leave by"
      answer. Golden fixture `fixtures/journeys/arrive-by.json` (direct, transfer chained backward
      to leave-by, impossible, last-connection across midnight); 4 web + 4 Kotlin tests.
      Web UI SHIPPED (after root-causing): the Plan draft now has a Leave now / Arrive by (time
      input) / Last train home mode selector. Verified in-browser: last train home = "leaves 01:30"
      (the real last M1 of the night), arrive-by 20:34 = "Leave by 20:15" (correct latest departure).
      ROOT CAUSE of the earlier wrong times was NOT the projector (`projectFromBundle` returns the
      full day correctly): (1) schedule bundles load async from the API, so a plan run before they
      arrive saw a thin timetable, and (2) the in-app browser had cached the OLD `web-journey-plan.js`
      (no time-mode branch -> forward -> earliest departure). Fixes: `?v=3` cache-bust on the journey
      scripts, and a schedules-loaded guard that shows "Schedules still loading" instead of a wrong
      time when no timetable could be built.
    - ANDROID arrive-by/last-train UI done: `JourneyPlanAdapter.toOptions` now routes to
      `assignScheduleArriveBy` / `lastConnection` (new `arriveByInstant` / `lastConnection` params);
      `PlanScreenRoute` has a Leave now / Arrive by (HH:MM field) / Last train mode row, a full-day
      timetable horizon for backward modes, and a "Leave by / Last train home leaves HH:MM" line.
      Verified on the emulator: Piraeus->Monastiraki last train = "leaves 02:21".
    - iOS arrive-by/last-train UI implemented (compiles, on-sim run blocked this session): `PlanFlow.swift`
      gained a Swift backward pass (latest catchable departure per leg, worst margin, tight<=179s,
      last-connection = latest available) and a `.segmented` Leave now / Arrive by / Last train
      Picker with a `.hourAndMinute` DatePicker, showing a "Leave by / Last train home leaves HH:MM"
      line (and honest no-train text). BUILD SUCCEEDED. Could NOT launch/drive on the simulator this
      session: the machine's `xcode-select` had flipped to CommandLineTools, so the simulator MCP
      cannot spawn `simctl` (restoring needs `sudo xcode-select -s /Applications/Xcode.app/...`,
      which requires the user's password). The Swift logic mirrors the emulator-verified Android
      path (last train "02:21") and the fixture-tested shared engine.
    - WEB k-shortest alternatives DONE: `SyrmosJourneyPlan.plan` generates candidate routes by
      line-banning (re-plan with each used line removed), schedules each with its own timetable
      (`buildTimetable(detailed)` callback per candidate), and runs them through the shared ranker
      (dedup identical leg sequences, order by objective, cap 3). The web Plan renders up to 3
      selectable option cards; clicking one mounts its route in the GO panel. Verified in-browser:
      Piraeus->Elliniko = "3 routes" (M3->M2 44min Tight [selected], M1->M2 45min, M1->M3->M2 48min),
      selection switches. Node tests assert distinct + capped + fastest-first.
    - ANDROID k-shortest DONE: `PlanJourneyUseCase.invoke(from,to,bannedLineIds)` re-plans with lines
      removed; `JourneyPlanAdapter.rankCandidates(...)` converts + schedules each candidate (own
      timetable) and runs the shared ranker (dedup + cap 3); `PlanScreenRoute` generates candidates
      (base + one per used line banned) and renders up to 3 selectable option cards. Verified on the
      emulator: Piraeus->Elliniko = "2 routes" (M1->M2 42min Tight [selected], M1->M3->M2 46min).
    - iOS k-shortest implemented (compiles; sim run still blocked): `JourneyPlanner.planDetailed`
      gained a `bannedLineIds` param; `JourneyPlanAdapter.planAll` generates candidates (base + one
      re-plan per used line banned), builds + schedules each via `buildPlanned`, dedups by line
      chain, orders by duration, caps at 3; `PlanView` renders up to 3 selectable option cards.
      BUILD SUCCEEDED. Not sim-verified (xcode-select still CommandLineTools).
    - iOS arrive-by/last-train + k-shortest VERIFIED on the simulator (xcode-select restored by the
      user). Piraeus->Elliniko last-train renders 3 distinct routes (M1->M2 tight 00:21, M3->M2
      comfortable 00:12, M1->M3->M2 comfortable 00:04) with localized "last train leaves"/"leave by"
      labels; arrive-by shows the DatePicker + backward recompute. NOTE: iOS sim taps are in the
      device POINT space the launch reports (402x874), not screenshot pixels.
    - SAVED JOURNEYS (S08 / J05) started, WEB DONE + browser-verified:
      - Shared golden fixture `fixtures/journeys/saved.json`: canonical versioned root
        `{schemaVersion, savedJourneys:[SavedJourney...]}`, storage key `syrmos.saved-journeys.v1`,
        pure-op cases (save/rename/remove/reorder) + decode ok/unsupported/corrupt. HARD de-dup
        invariant: at most one saved journey per (fromId,toId); re-saving a pair updates in place
        (keeps id), refreshes label/createdAt, moves to top. Mirrors Kotlin `SavedJourney`.
      - Web `web-saved-journeys.js` (`SyrmosSavedJourneys`): pure save/rename/remove/reorder/
        encode/decode + a localStorage-backed `createStore`; atomic/versioned decode never
        overwrites an unsupported/corrupt blob. Wired into the Plan workspace (`web-map.js`
        goPlan): "Save journey" on a result (flips to "Saved", kept in sync via `syncSaveButton`),
        a "Saved journeys" list (tap row = load+re-plan, rename via prompt, delete with 5s Undo),
        empty state; 4-lang. CSS in `web-map.css`. Test `web-tests/saved-journeys.test.js` (7)
        drives every fixture case + de-dup + store persistence; full web suite 152 green.
      - Browser-verified in-app: save -> row+"Saved"; one row per pair (de-dup); delete -> Undo
        banner + empty state + Save button re-enables; Undo restores (localStorage round-trip).
    - Still pending in P: mirror saved journeys onto Android + iOS (shared `SavedJourneyStore`);
      broaden interchange-node projection coverage further; S03 selector, S05 detail.
- [ ] Phase G / R / N / Z (not started)

Update this log as slices land. Keep "implemented / tested / deferred / SDK-or-device-blocked"
separated, per the prompt's definition of done.
