# Syrmos 3.0.0 — close the seven remaining visual and journey findings

Use the following as an implementation prompt. This document is a specification; creating it does not fix or validate the app.

---

You are working in `/Users/peterdsp/git/Syrmos`. Complete the remaining 3.0.0 findings **8, 10, 2, 3, 4, 6 and 7**. Implement the engineering fixes, prepare and integrate native Greek review, resolve the ranking decision with the product owner, and verify the resulting behaviour. Work through the task; do not stop after an audit or a proposed plan.

Keep this a focused 3.0.0 correction pass. Preserve Syrmos branding, navigation, shared design tokens, working translations, saved journeys and active GO sessions. Preserve unrelated local changes. Do not bump the release version, upload a build, submit to stores or deploy as part of this task.

## 1. Establish the current evidence

Read:

- `docs/screenshots/release-3.0.0/FINDINGS.md`
- `docs/screenshots/release-3.0.0/README.md` and `manifest.tsv`
- `docs/plans/3.0.0-IMPLEMENTATION-PROMPT.md`, especially S03–S06 and sections 7–8
- `docs/plans/3.0.0-implementation-roadmap.md`, especially section 2.1
- `docs/ops/RELEASE-3.0.0-GATE.md`
- Applicable repository instructions and the actual implementation/test files before editing.

These source pointers were checked at commit `4a8a53d5`. Recheck the working tree and reproduce each finding on the current build; historical documentation is not proof of current runtime behaviour. Keep the original finding numbers in the work log.

The reported Greek count is **254 of 802 eligible iOS literals, approximately 31%** from the previous audit, not a new measurement. The audit records **11 vs 12 stops**, while the latest report says **3 vs 4**: reproduce the semantic mismatch rather than hardcoding either example. Finding 6 needs a fresh scrolled capture; its original scrolled images are not in the baseline set.

These findings originated on iOS. Fix shared semantics consistently in Swift, Kotlin/Android and the served web client where affected. The served web app lives in `composeApp/src/wasmJsMain/resources/`; do not implement web fixes in an unused Compose shell. Inspect equivalent surfaces before declaring cross-platform coverage. Preserve the current web access policy.

Keep already-fixed findings 1 and 9 covered against regression. Finding 5 was retracted and is outside this task unless new evidence establishes a separate defect.

## 2. Finding 8 — native-quality Greek copy

Start the human review work early, then continue the independent engineering fixes while it is pending.

Inspect `iosApp/iosApp/Core/Localization/Localization.swift` and inline display copy, especially:

- `iosApp/iosApp/Views/Lines/RailPulseDetailViews.swift`
- `iosApp/iosApp/Views/Lines/ExploreRailPulseView.swift`
- `iosApp/iosApp/Views/Settings/SettingsView.swift`
- `iosApp/iosApp/Views/Lines/LinesView.swift`
- Home, airport and shared display components that reuse this copy.

Create `docs/qa/3.0.0-greek-copy-review.md` containing a reviewable inventory: stable key or source location, screen/context, current Greek, proposed Greek, English meaning, placeholder/plural constraints, and review status. Include all flagged candidates and inspect nearby copy for partial errors that the original no-accent heuristic misses. Link representative screenshots. Record the inventory method and current totals separately from the historical count.

Have a **native Greek speaker** review spelling, tonos, grammar, transit terminology, casing, plural forms and natural wording in context. Suggested corrections are drafts until reviewed. Include the known cases:

- `Ελλαδα, ζωντανα και με τη δυναμη της κοινοτητας` → `Ελλάδα, ζωντανά και με τη δύναμη της κοινότητας`
- `Προορισμος, σταθμος, γραμμη η τρενο...` → `Προορισμός, σταθμός, γραμμή ή τρένο...`

Apply approved text consistently wherever reused. Preserve interpolation tokens and legitimate proper names. Do not run an indiscriminate accent-insertion or replacement script, treat every unaccented Greek string as wrong, or use an AI review as evidence of native-speaker approval. Intentional all-caps labels, acronyms and monosyllables need context.

Keep display text separate from normalized search/parser keys. In particular, preserve deliberate normalization in `AriadneParser.swift`, IDs, aliases, URLs and wire values. Search must continue to accept both accented and unaccented input. Preserve the existing `uppercasedForDisplay` correction from finding 9 and the distinction between all-caps and title case.

Add focused regression coverage for approved high-risk examples and placeholder/plural preservation. A candidate detector may flag new suspicious display strings, with documented exceptions; it cannot certify correct Greek. Record reviewer identity/date only when actually supplied. If no reviewer is available, finish the review pack and all independent fixes, then leave finding 8 explicitly **awaiting native review**, not fixed. Do not contact anyone without authorization.

**Acceptance:** all confirmed Greek copy defects in scope have approved corrections applied and inspected on their screens; no known defective strings remain untracked. A lower heuristic count alone does not close the finding. Albanian concerns may be recorded separately, without expanding this task into an unrequested translation rewrite.

## 3. Finding 10 — localized place names in Greek cards

Trace the name from its source to the actual display field:

- `iosApp/iosApp/Views/Home/HomeView.swift`: direction labels currently interpolate `next.direction`.
- `iosApp/iosApp/Core/Schedule/TransitData.swift` and `StationCoordinates.swift`: service directions and localized station data.
- `iosApp/iosApp/Core/Schedule/AirportData.swift`: `AirportHub.name` is currently a plain string; `AirportL10n` already exists.
- `iosApp/iosApp/Features/Timetables/TimetablesView.swift`: `AirportHubHero` currently displays `hub.name`.
- Android counterpart: `feature/schedule/src/commonMain/kotlin/com/syrmos/feature/schedule/AirportHubScreen.kt` also has a plain airport-name field; check and correct its equivalent Greek presentation.

Resolve destinations through stable station/destination identity and the existing localized data. If a legacy direction only supplies text, use a deliberate alias mapping with line/direction context. Do not use a global English-to-Greek string replacement or assume similarly named localization helpers support that translation direction.

Give airport proper names the existing localized model treatment; first locate authoritative Greek data, and if a field is missing, add the reviewed value centrally. Greek Home should display **προς Ανθούπολη** and the Athens airport hero should display **Ελευθέριος Βενιζέλος**. Keep IDs and legitimate codes such as M3, A1 and ATH unchanged. Define a safe fallback for genuinely missing localized names without inventing a translation.

**Acceptance:** correct names appear on Home and Airport, including after switching language without restarting; equivalent cards using the same data are consistent. Test English and Greek, both airport hubs, missing-name fallback and any legacy direction aliases. Other supported languages retain their correct display values. Verify the result in runtime captures, not only string tests.

## 4. Finding 2 — one distinguishable selector row per real station choice

Start with `JourneyPlanAdapter.allStations`, the selector and `matches` in `iosApp/iosApp/Features/Assistant/PlanFlow.swift`; inspect `JourneyPlanner.swift`, station identity data and interchange mappings. Reproduce the `Kifis` query in S03. The current data includes `A1_KIF` and `A2_KIF` with the same Kifisias name and coordinates.

Introduce or reuse a stable **station group identity** for presentation, retaining every underlying routable stop ID and line membership. Collapse multiple representations of the same physical station choice into one row with its line badges. Keep genuinely distinct stations separate, with a useful region/operator/line subtitle when names collide.

Do not deduplicate by localized display name, list index or proximity alone. Names and coordinates are evidence, not sufficient identity rules. Specifically, **Kifissia and Kifisias must remain distinct**. Use verified station mappings; investigate ambiguous records instead of silently merging them.

Selection must retain access to every valid service from the group. Do not take an arbitrary first stop ID and make another line unreachable. Carry a group to the planner or resolve its member stops deliberately. Preserve compatibility with saved endpoints, recents and active journeys; add migration only if persisted identity must change.

**Acceptance:** `Kifis`, Greek equivalents, accented/unaccented searches and cleared queries produce stable, distinguishable rows. Cover duplicate input IDs, shared physical stations, same-name stations in different places, interchanges and reordered input. Test that every represented line remains routable after grouping, and selected origin/destination survives locale changes and restoration. Audit equivalent Android/web selectors for the same defect and use shared fixtures where semantics are shared.

## 5. Finding 3 — explicit, consistent stop-count semantics

Current code measures two different things:

- S05 `JourneyDetail.timeline`: **intermediate stops**, `max(0, orderedStopIds.count - 2)`.
- S06 GO: **stops remaining to alight**, `lastStopIndex - currentStopIndex`, including the current leg's alight stop.

For `[A, B, C, D, E]`, boarding at A means **3 intermediate stops** and **4 stops to E**. Fix the ambiguous wording and presentation; do not force the numbers to match by corrupting GO arithmetic.

Use explicit localized labels equivalent to **“3 intermediate stops”** in S05 and **“4 stops to [this leg's alight station]”** in S06. On a transfer journey this may be an interchange; use the final destination only on the final leg. At arrival use the correct arrival state. The S05 disclosure must contain precisely the intermediate stations it counts. Obtain native review for changed Greek labels alongside finding 8.

Source targets:

- iOS: `Core/Journey/JourneyDetail.swift`, `Features/Assistant/PlanFlow.swift` (`StopsDisclosure`), `Features/Assistant/JourneyGuidance.swift`, and `Features/Go/GoJourneyView.swift`, under `iosApp/iosApp/`.
- Shared: `core/domain/src/commonMain/kotlin/com/syrmos/core/domain/journey/JourneyDetail.kt` and `core/domain/src/commonMain/kotlin/com/syrmos/core/domain/go/GoGuidance.kt`.
- Android UI: `composeApp/src/commonMain/kotlin/com/syrmos/app/screen/PlanScreenRoute.kt` and `GoJourneyScreenRoute.kt`.
- Web: `web-journey-detail.js`, `web-map.js`, `web-go.js` and `web-go-panel.js` in the served resources directory.

**Acceptance:** shared cases cover a five-stop ride, two-stop ride, reverse direction, transfer boundary, manual progression, arrival and resume. S05→S06 uses the same selected itinerary and ordered leg. GO decrements only when progress advances; get-off-next cues, alerts and saved progress retain their existing meaning. Use `fixtures/journeys/detail.json` and `fixtures/go-guidance/cases.json` with the existing Swift/Kotlin/web tests. Include singular/plural labels in all four locales.

## 6. Finding 4 — keep floating actions clear of content

Reproduce both cases: Explore's **Plan a journey** pill covers the `90 m` and following time chips; Airport's Ariadne launcher covers route-overview stop labels.

Inspect `iosApp/iosApp/Views/Lines/LinesView.swift` (the bottom-trailing Plan overlay and fixed bottom offset), `ExploreRailPulseView.swift`, `iosApp/iosApp/App/SyrmosApp.swift` (`assistantLauncher`, tab insets and readable content), and `iosApp/iosApp/Features/Timetables/TimetablesView.swift` (scroll content and route overview).

Give the page and shell one coherent layout contract for bottom actions, tab chrome and scrollable content. Use measured layout/safe areas or an in-flow action area so content and controls occupy separate usable space. Reserve the actual height required as text size, locale and window geometry change. Preserve the visual design and access to both Plan and Ariadne.

Do not solve this with another device-specific bottom offset, shrinking text, hiding chips, disabling interaction, or an invisible full-width layer that intercepts taps. A bottom spacer alone is insufficient if the visible chip row is still underneath the button.

**Acceptance:** every time chip is visible and tappable when its row is in view; every airport stop label can be read and reached; launchers and tab controls still work. Check initial and scrolled states, horizontal route scrolling, keyboard-open state, compact/short windows, larger windows and accessibility text sizes. Verify taps on the previously covered controls in the running app.

## 7. Finding 6 — readable scrolling beneath the More header

Inspect `iosApp/iosApp/Views/Settings/SettingsView.swift`, `iosApp/iosApp/DesignSystem/CompactTabHeader.swift` and the surrounding app shell. The current view already has a top `safeAreaInset`; adding another inset without reproducing the scroll-under behaviour is not a sufficient fix.

Make the header and list viewport cooperate: reserve the actual header height, account for safe areas, and clip/cover the scrolling region appropriately so row text never bleeds half-legibly through the title pill. Use an appropriate background and Reduce Transparency fallback. Prefer the smallest shared header correction that preserves the design; check other users of `CompactTabHeader` if its behaviour changes.

**Acceptance:** slowly scroll **Leave-by reminders** and **Saved departures** past the header in both directions. Rows are readable within the content viewport and properly occluded when leaving it; they never compete with “More” through the capsule. Capture several scroll positions in both themes, Greek and English, default and large text, including accessibility focus and tap behaviour. Verify all rows remain reachable and the header does not overlap the first row at rest.

## 8. Finding 7 — ranking parity, then an explicit product decision

Separate a current contract violation from a proposed policy change.

**First, restore the existing contract.** Section 7.2 specifies requested objective, then arrival, changes, walking and stable ID. Kotlin `JourneyRanker.kt` and web `web-journey-ranker.js` implement that chain; iOS `JourneyPlanAdapter.planAll` in `PlanFlow.swift` currently sorts only by duration. Reproduce the inputs, repair the iOS parity gap, and test the same candidates across clients. Unknown metrics must remain unknown and follow the documented fallback, never become zero.

For the audited S04 case, record the actual unrounded duration, departure/arrival, transfer count, walking availability, feasibility and margin for M1→A1, direct A1 and M1→A2. Two cards showing “~48 min” do not prove equal journeys. Fix misleading precision or missing explanation without inventing timing data.

**Then present a concrete policy decision to the product owner:**

1. **Keep objective-first:** fastest may rank a genuinely faster tight route first; display its risk and the real time trade-off clearly. Equal metrics use the existing deterministic tie-breaks.
2. **Prefer comfortable journeys:** change the default recommendation to prioritize known comfortable options, with an explicit policy for the additional journey time accepted and for unknown feasibility. Preserve honest objective labels; a slower recommendation cannot be labelled “Fastest.”

Prepare a comparison using the reproduced case and examples with a larger time penalty, unknown feasibility and no comfortable alternative. Recommend option 2 for the default recommendation **if the owner accepts its time trade-off**, but do not invent a minute penalty, cutoff, new default mode or ranking preference. Obtain an explicit decision before changing that product policy; continue independent fixes while it is pending. If the session already contains an applicable decision, use it without asking again.

Record the chosen ordering, trade-offs, unknown/missed/closed handling and badge rules in `docs/qa/3.0.0-ranking-decision.md`; update the roadmap/spec and implement the approved rule in all affected clients. A decision to retain objective-first resolves the product question only with the owner's acceptance and verified clear presentation. Pending approval stays **awaiting product decision**.

Preserve the existing feasibility thresholds: negative margin is missed, 0–179 seconds tight, 180+ comfortable; a required missing term means unknown. A direct ride without known boarding access time is not evidence of a comfortable connection. Keep scheduled/estimated source status visible. Missed or closed journeys must not become actionable recommendations.

**Acceptance:** extend `fixtures/journeys/ranking.json` and existing ranker/planner tests. Cover exact ties, equal rounded durations with unequal underlying times, comfortable versus tight, unknown metrics, 179/180-second boundaries, shuffled candidate input, deduplication and the three-option cap. Use a deterministic total ordering, not a pairwise “within a few minutes” comparison that can violate transitivity. Preserve the selected journey by stable identity during refresh. Verify S04→S05→S06 still starts the selected option.

## 9. Verification and evidence

Implement in focused changes: reproduce and start Greek review; repair names and station identity; clarify stop semantics; repair overlays and header scrolling; repair ranking parity; integrate the approved copy/policy; run final checks and captures. Parallelize independent work without conflicting file edits.

Run relevant existing tests and add targeted behavioural coverage for the changed rules. Starting points:

- iOS: `JourneyDetailTests`, `JourneyGuidanceTests`, `JourneyPlannerConnectTests`, saved-journey tests and new focused name/identity/ranking coverage where needed.
- Kotlin: affected common/model/domain/data tests, especially `JourneyDetailTest`, `JourneyRankerTest` and `GoGuidanceTest`.
- Web: `web-tests/journey-detail.test.js`, `journey-ranking.test.js`, `go-guidance.test.js`, affected localization/selector tests, then the full web suite for shared JS changes.
- Discover current Xcode schemes/simulators and Gradle tasks; build affected clients. Existing entry points include `node --test web-tests/*.test.js`, `./gradlew :core:domain:allTests`, `./gradlew :androidApp:assembleDebug`, and `xcodebuild -list -project iosApp/Syrmos.xcodeproj`. Run `python3 scripts/verify-bundles.py` if bundled/generated data changes. Confirm tool availability; old release-gate environment notes may be stale.

Use `scripts/capture-baselines.sh` on a dedicated test simulator; its setup resets app and shared test state. Preserve before-evidence, then recapture affected S01, S03, S04, S05, S06 and S09 states. Reuse the pinned Athens clock (`2026-09-16T08:42:00+03:00`), fixed location and bundled offline data; verify the harness receipt and metadata. Do not alter seed data merely to hide a defect.

Minimum focused visual checks:

- iOS C402: Greek and English, light and dark, all affected screens; explicit More scroll positions and previously covered controls.
- Compact/narrow and short layouts, a larger/resizable layout, keyboard-open selectors, Dynamic Type XXXL and Reduce Transparency for changed layout surfaces. Record the actual tested geometry; do not relabel C402 as an exact C390 test or claim an unavailable foldable runtime was tested.
- Smoke-check Albanian and Italian for changed labels and expansion. For affected Android/web semantics, use the same journey fixtures and inspect the rendered labels, ordering and selection; test Android font scale 2.0/web zoom 200% where those layouts change.
- Capture the same selected leg before and after starting GO, plus intermediate-stop disclosure and progression.

Inspect every changed capture. A successful build, source-string assertion or screenshot file alone does not prove the fix. Compare each platform against its own baseline; document intentional copy/layout changes instead of masking them. Keep online/live coverage and unavailable checks explicitly separate from deterministic offline evidence. Do not claim completion of the entire release matrix from this focused pass.

## 10. Deliverables and closure

Deliver the code, targeted tests/fixtures, native Greek review record, ranking decision record and inspected screenshots with reproducible metadata. Update `FINDINGS.md`, the roadmap and release gate only to reflect what is now evidenced; preserve the historical findings and link the new proof.

End with one row per finding: **root cause · change · tests/runtime evidence · status · remaining dependency**. Use precise statuses: fixed and verified, implemented awaiting verification, awaiting native review, or awaiting product decision. Report failed/unavailable checks with their actual reason. Do not mark all seven closed while either human dependency remains unresolved.

Success means readable, native-quality Greek; localized place names; distinguishable station choices without losing routes; explicit stop-count meanings; accessible controls and scrolling; and a consistent ranking policy the product owner has actually chosen.
